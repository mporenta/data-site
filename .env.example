# ==============================================
# Docker Deployment Configuration
# ==============================================

# Domain Configuration (for production deployment)
DOMAIN=data.porenta.us
EMAIL=your-email@example.com

# Let's Encrypt Configuration
# Set STAGING=1 for testing (avoids rate limits)
# Set STAGING=0 for production (real certificates)
STAGING=0

# ==============================================
# Application Configuration
# ==============================================

# Next.js Public Variables (build-time, visible to browsers)
# For development: http://localhost:8000
# For Docker local: http://python-api:8000
# For production: https://data.porenta.us/api
NEXT_PUBLIC_API_URL=https://data.porenta.us/api

# Snowflake Configuration (for Python API)
SNOWFLAKE_ACCOUNT=your-account.region
SNOWFLAKE_USER=your-username
SNOWFLAKE_PASSWORD=your-password
SNOWFLAKE_WAREHOUSE=your-warehouse
SNOWFLAKE_DATABASE=your-database
SNOWFLAKE_SCHEMA=your-schema

# Okta Configuration (for future use)
OKTA_ISSUER=https://your-domain.okta.com/oauth2/default
OKTA_AUDIENCE=your-audience
OKTA_CLIENT_ID=your-client-id
OKTA_CLIENT_SECRET=your-client-secret

# ==============================================
# Important Notes
# ==============================================
# - Copy this file to .env and fill in your actual values
# - Never commit .env to version control
# - NEXT_PUBLIC_* variables are inlined at build time
# - Server-side variables can be changed by restarting containers
