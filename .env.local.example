# ==============================================
# LOCAL Docker Desktop Development Configuration
# ==============================================
#
# This is an example environment file for local Docker Desktop development.
# Copy this file to .env.local and fill in your actual values.
#
# Usage with docker-compose-local.yml:
#   cp .env.local.example .env.local
#   docker compose -f docker-compose-local.yml --env-file .env.local up -d --build

# ==============================================
# Application Configuration
# ==============================================

# Next.js Public Variables (build-time, visible to browsers)
# For local Docker: http://localhost:8000
NEXT_PUBLIC_API_URL=http://localhost:8000

# Internal API URL (server-side only, not exposed to browsers)
# For local Docker: http://python-api:8000 (Docker service name)
API_URL=http://python-api:8000

# ==============================================
# Database Configuration
# ==============================================
# Leave empty if using CSV mock data

# Snowflake Configuration (optional for local development)
SNOWFLAKE_ACCOUNT=
SNOWFLAKE_USER=
SNOWFLAKE_PASSWORD=
SNOWFLAKE_WAREHOUSE=
SNOWFLAKE_DATABASE=
SNOWFLAKE_SCHEMA=

# AWS RDS Configuration (future - not yet implemented)
# RDS_HOST=
# RDS_PORT=5432
# RDS_DATABASE=
# RDS_USER=
# RDS_PASSWORD=

# ==============================================
# Authentication Configuration
# ==============================================
# Okta Configuration (not needed for local development)
OKTA_ISSUER=https://aptive.okta.com/oauth2/default
OKTA_AUDIENCE=
OKTA_CLIENT_ID=
OKTA_CLIENT_SECRET=

# ==============================================
# Important Notes
# ==============================================
# - This file is for LOCAL development only
# - For production, use .env with docker-compose.yml
# - CSV mock data works without any database configuration
# - Okta auth is not enforced in local development
# - NEXT_PUBLIC_* variables are inlined at build time
